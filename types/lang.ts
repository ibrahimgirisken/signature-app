export interface Lang {
  id: string;
  langCode: string;
  title: string;
  image: string;
}

export interface LangResponse {
  id: string;
  langCode: string;
  title: string;
  image: string;
}

export interface LangUpdate {
  id: string;
  langCode: string;
  title: string;
  image: string;
}

export interface LangCreate {
  id: string;
  langCode: string;
  title: string;
  image: string;
}
